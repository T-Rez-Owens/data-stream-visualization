'use strict'



describe('Test the root path', () => {
    
    test('It should response the GET method', () => {
        expect.assertions(1);
    });
})
describe('Test the root path 2', () => {
    test('It should response the GET method', () => {
    });
})
